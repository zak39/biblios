{% extends 'base.html.twig' %}

{% block title %}Liste d'éditeurs{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<section class="container px-4">
    <div class="w-50 m-auto">
        <h1>Liste d'éditeurs</h1>

        {% if is_granted('ROLE_AJOUT_DE_LIVRE') %}
            <twig:Admin:ButtonNewEntity
                name="Créer un éditeur"
                action="app_admin_editor_new" />
        {% endif %}

        <ul class="list-group list-group-flush">
            {% for editor in editors %}
                <twig:Admin:EditorCard id="{{ editor.id }}" name="{{ editor.name }}" />
                {% else %}
                    <li>Aucun enregistrement en base de données.</li>
            {% endfor %}
        </ul>
        {{ pagerfanta(editors) }}
    </div>
</section>
{% endblock %}
