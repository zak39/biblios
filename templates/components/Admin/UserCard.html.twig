<div class="card mb-1">
    <div class="card-body">
        <div class="card-title d-flex justify-content-between">
            <a href="{{ path('app_admin_user_show', { id: id }) }}" class="stretched-link link-dark">
                <h4 class="mb-1">{{ username }}</h4>
            </a>
            <small class="text-muted">Identifiant : {{ id }}</small>
        </div>
    </div>
</div>
